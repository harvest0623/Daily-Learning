# 多模态
- deepseek 不是多模态的
- 豆包是多模态的

# Agent 
- Agent 是一种人为打造的，可以跟用户的指令，进行任务分析，编排，执行的智能体

# Anthropic

# claudeCode
1. 可以内置子 agent，可以人为配置子 agent 用于执行不同的任务
2. 上下文受限，只能处理当前上下文中的任务
3. 可以与用户进行交互，用户可以与 agent 进行对话，获取信息
4. 可以与外部系统进行交互，例如数据库，文件系统，网络等